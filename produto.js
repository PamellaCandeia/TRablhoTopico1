class Produto {
    
    constructor(codigo, nome, descricao, precoUnit, foto) {
        this.codigo = codigo
        this.nome = nome
        this.descricao = descricao
        this.precoUnit = precoUnit
        this.foto = foto
    }
}